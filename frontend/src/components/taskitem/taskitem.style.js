import { makeStyles } from "@mui/styles";
export const useStyles = makeStyles({
    container: {
        minHeight: 30,
        width: "100%",
        display: "flex",
        alignItems: "center",
        marginBottom: 10,
    }
});